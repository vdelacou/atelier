# Class-to-Module Translation Catalogue

Folded into `references/design-patterns.md` on 2026-09-03: the basic translations (value object, interface, service with dependencies, entity) open that file and the quick-reference table closes it; the strategy, factory, decorator, observer and command translations were already its GoF entries. This stub stays for one release so a consumer pinned at an older hash still resolves the path; read `references/design-patterns.md`.
