# macOS
.DS_Store

# editors
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# claude-code local state
.claude/settings.local.json
.claude/lessons.local.md

# packaged skill artefacts
dist/
*.skill

# skill-creator eval workspaces (results, logs, html reports)
skills/*-workspace/

# node (kept for user scratch repos in this workspace)
node_modules/
*.log
*.tgz
.eslintcache

# python bytecode (trigger-eval runner)
__pycache__/
.judge-last.json

# swarm-forge runtime, when ./swarm runs in this checkout (the shared articles
# and scripts come from swarm-forge main at launch; the pack ships only its own)
.swarmforge/
.worktrees/
swarmforge/scripts/
swarmforge/constitution/articles/engineering.prompt
swarmforge/constitution/articles/workflow.prompt
swarmforge/constitution/articles/handoffs.prompt
